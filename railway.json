{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @psychology/types build && pnpm --filter @psychology/backend prisma:generate && pnpm --filter @psychology/backend build"
  },
  "deploy": {
    "startCommand": "cd apps/backend && pnpm prisma:migrate:prod && node dist/main.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
